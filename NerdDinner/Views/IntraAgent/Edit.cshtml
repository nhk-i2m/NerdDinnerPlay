@model NerdDinner.Models.IntraAgent

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>IntraAgent</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.IntraUserName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.IntraUserName)
            @Html.ValidationMessageFor(model => model.IntraUserName)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.IntraPassword)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.IntraPassword)
            @Html.ValidationMessageFor(model => model.IntraPassword)
        </div>
        <p>
            <input type="submit" value="Ok" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
